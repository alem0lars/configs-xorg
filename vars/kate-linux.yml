# => inherits: generic, system-level, wacom, with-urxvt <= #

xorg:
  main_video_card:
    driver:      intel
    vbe_restore: true

fonts:
  main:
    name: Hack
    size: 11

urxvt:
  url_launcher: chromium

colorscheme:
  name: jellybeans
